% ==============================================
% CFD Tutorial Template: OpenFOAM Terminal Basics
% Class: scrartcl (KOMA-Script) - optimized for tutorials
% Content: pitzDaily with simpleFoam
% ==============================================

\documentclass[
    parskip=half,      % Space between paragraphs (better for steps)
    DIV=12,            % Optimal margins for readability
    headings=normal,   % Professional heading sizes
    fontsize=11pt,     % Slightly larger for screen reading
    english            % Language setting
]{scrartcl}

\input{../preamble.tex}

% ========== DOCUMENT START ==========
\begin{document}

% ========== TITLE PAGE ==========
\title{Tutorial \#2: Water-air Ejector}
\subtitle{Multiphase Transient Flow}
\subject{Workshop on Computational Fluid Dynamics (CFD) \\ with OpenFOAM \\ IMP-PAN Gda\'nsk}
\author{Robert Castilla \\ CATMech - Universitat Politècnica de Catalunya \\ Department of Fluid Mechanics}
\date{2025-26 \\ Version 1.0}



\maketitle

% ========== ABSTRACT ==========
\begin{abstract}
This tutorial introduces the student to simulation of multiphase flow with a water-air ejector
as example. Two main approaches, Volume Of Fluid and Euler-Euler, are covered. The mesh is 
generated with the help of a python script, that facilitates possible parametric
study with modifications of geometry.  The entrainment 
ratio is computed for both models and compared.
\end{abstract}

\tableofcontents
\newpage

\fbox{
	\parbox{15cm}{

\section*{Learning Objectives}
By the end of this tutorial the student will be able to
	
	\begin{enumerate}
		\item \textbf{Understand} basic differences between Volume Of Fluid and Euler-Euler 
		approaches 
		
		\item \textbf{Run} a simulation of a water ejector with VOF and EE approaches
		
		\item \textbf{Compute and analyze} entrainment ratio M 
		
		\item \textbf{Visualize} basic results of multiphase ejector with \terminalcmd{paraview}
	\end{enumerate}
}
}
% ========== SECTION 1: INTRODUCTION ==========
\section{Introduction and Theoretical Background}

In this section a water-air ejector will be simulated. A water-air ejector is a simple 
device that uses high speed water to provoke  pressure depression and entrain air from 
another inlet. Both water and air are mixed in the mixing chamber and expelled through a 
diffuser to recover the pressure lost.

Two approaches will be explored to make this simulation. 

The first approach is the \textbf{Volume Of Fluid (VOF)} method. The equations are basically the same
as in the previous session, and the interface is tracked with the phase fraction 
$\alpha \in [0,1]$,

\begin{equation}
\frac{\partial (\rho \mathbf{U})}{\partial t} + \nabla \cdot (\rho \mathbf{U} \mathbf{U}) = 
-\nabla p + \nabla \cdot \left[ \mu \left( \nabla \mathbf{U} + \nabla \mathbf{U}^T \right) \right] + \rho \mathbf{g} + \mathbf{F}_\sigma
\end{equation}  
with the equation of the phase fraction transport
\[
\frac{\partial \alpha}{\partial t} + \nabla \cdot (\alpha \mathbf{U}) = 0
\]
and the mixture properties in each cell
\[
\rho = \alpha \rho_w + (1-\alpha) \rho_a, \qquad 
\mu = \alpha \mu_w + (1-\alpha) \mu_a
\]

Surface tension force is computed with the Continuum Surface Force (CSF) model,
\[
\mathbf{F}_\sigma = \sigma \kappa \nabla \alpha, \qquad 
\kappa = -\nabla \cdot \left( \frac{\nabla \alpha}{|\nabla \alpha|} \right)
\]

Alternatively, the second approach considered here is the \textbf{Euler-Euler} method, where each phase 
is treated as interpenetrating continua, with interphase terms for momentum, heat and mass 
transfer. In this case, several phases can be included, with phase fractions $\alpha_k$, 

\[
\sum_{k=1}^{N} \alpha_k = 1
\]

For each phase, continuity has to be fulfilled,
\begin{equation}
	\frac{\partial (\alpha_k \rho_k)}{\partial t} + \nabla \cdot (\alpha_k \rho_k \mathbf{U}_k) = 0
\end{equation}
as well as the momentum equation,
\begin{equation}
\frac{\partial (\alpha_k \rho_k \mathbf{U}_k)}{\partial t} + \nabla \cdot (\alpha_k \rho_k \mathbf{U}_k \mathbf{U}_k) = 
-\alpha_k \nabla p + \nabla \cdot \left[ \alpha_k \mu_{k,\text{eff}} \left( \nabla \mathbf{U}_k + \nabla \mathbf{U}_k^T \right) \right] + \alpha_k \rho_k \mathbf{g} + \mathbf{M}_{k}
\end{equation}
where $\mathbf{M}_{k}$ is the interphase momentum transfer, that includes terms for drag, 
lift, virtual mass and turbulent dispersion.



\begin{table}[H]
	\centering
	\caption{Comparison of VOF and Euler-Euler approaches}
	\label{tab:vof_euler_comparison}
	\begin{tabular}{|p{4cm}|p{4cm}|p{4cm}|}
		\hline
		\textbf{Feature} & \textbf{VOF} & \textbf{Euler-Euler} \\
		\hline
		Interface representation & Sharp (captured) & Diffuse (interpenetrating) \\
		\hline
		Momentum equations & One (mixture) & One per phase \\
		\hline
		Interphase forces & Surface tension only & Drag, lift, virtual mass, turbulent dispersion \\
		\hline
		Turbulence & Single model (mixture properties) & Per-phase models \\
		\hline
		Best for & Stratified, free-surface flows & Dispersed, bubbly, particle-laden flows \\
		\hline
		Computational cost & Lower & Higher \\
		\hline
	\end{tabular}
\end{table}
\subsection{Case Overview}

The geometry of the ejector is the one published in the paper by Zhang et al. \footnote{Zhang, Zhao, Daotong Chong, and Junjie Yan. 2012. “Modeling and Experimental Investigation on Water-Driven Steam Injector for Waste Heat Recovery.” Applied Thermal Engineering 40. https://doi.org/10.1016/j.applthermaleng.2012.02.006.}


\section{Case Setup}

\subsection{Python virtual environment}

To facilitate the generation of the mesh with \terminalcmd{blockMesh} we are going to 
use a python code. It is convenient to create first a user virtual environment, so
that packages can be locally installed. Here follows the procedure

\begin{lstlisting}[style=terminal]
# Create the virtual environment. Here the name is 'my_env' but it can be any name
python -m venv my_env
# Activate the virtual environment
source ./my_env/bin/activate
# From now on a '(my_env)' will be shown with prompt
# To deactivate the virtual environment, just type
deactivate 
\end{lstlisting}

The module 
\href{https://github.com/takaakiaoki/ofblockmeshdicthelper}{ofblockmeshdicthelper}, by 
A. Takaaki will be used. It has to be installed in the environment with

\begin{lstlisting}[style=terminal]
pip install 'ofblockmeshdicthelper @git+https://github.com/takaakiaoki/ofblockmeshdicthelper.git'
\end{lstlisting}

If some other module is needed, just install it with \terminalcmd{pip install <module name>}.


\section{Part A: Volume of Fluid Approach} 

The clean case can be downloaded from the GitHub repository,

% TODO: instruction to download the case


\subsection{Running the Simulation}

The simulation process is split into two automated phases: mesh generation and solver
execution. Both phases are implemented in shell scripts, to ensure consistent
and reproducible workflow.

\subsubsection{Mesh generation}

The first step is to create the \filename{system/blockMeshDict} file. It is
done with the python code \filename{blockMeshDictGen.py}, that load the 
\terminalcmd{ofblockmeshdicthelper} mentioned in the previous section.


Check that the python virtual environment is activated and run
\begin{lstlisting}[style=terminal]
python blockMeshDictGen.py
\end{lstlisting}
The file \filename{system/blockMeshDict} has been created.

Run the script for the mesh generation

\begin{lstlisting}[style=terminal]
./genMesh.scr
\end{lstlisting}

\warning{It the command fails, it is probable that it is not executable. Change it 
with}
\begin{lstlisting}[style=terminal]
chmod +x ./genMesh.scr
\end{lstlisting}

Have a look at the script and discuss the meaning of each command. Check the output
of the \terminalcmd{checkMesh} command and 
visualize the mesh with \terminalcmd{paraFoam}

\subsubsection{Initialization and solver execution}

The \filename{runCase.scr} performs several tasks:

\begin{enumerate}
	\item It copies the content of the folder \filename{0.orig} to the forlde 
	\filename{0}.
	\item It initializes the phase distribution with the command 
	\terminalcmd{setFields} and the dictionary \filename{system/setFieldsDict}
	\item It runs the \terminalcmd{interFoam} solver. 
\end{enumerate}

The simulation will take about 10 minutes. In the meanwhile the configuration files of
the case can be studied:

\begin{itemize}
	\item \filename{system/controlDict} and functions definitions
	\item \filename{0.orig/*}
	\item \filename{constant/*}
	\item \filename{system/fvSolution} and \filename{system/fvSchemes}
\end{itemize}




\subsection{Post-Processing in Terminal}

Residuals of the simulation can be check with

\begin{lstlisting}[style=terminal]
foamMonitor -l postProcessing/solverInfo/0/solverInfo.dat
\end{lstlisting}

\begin{center}
	\includegraphics[width=\linewidth]{Figures/VOFresiduals}
\end{center}


Also flow rates from main and secondary inlets

\begin{lstlisting}[style=terminal]
foamMonitor postProcessing/flowRatePatch/0/surfaceFieldValue.dat
\end{lstlisting}
\begin{center}
	\includegraphics[width=\linewidth]{Figures/VOFflowRate}
\end{center}

\begin{lstlisting}[style=terminal]
	foamMonitor postProcessing/flowRatePatchMain/0/surfaceFieldValue.dat
\end{lstlisting}
\begin{center}
	\includegraphics[width=\linewidth]{Figures/VOFflowRateMain}
\end{center}

Note that the flow rate in main secondary inlets are\footnote{These are the flow rate
for \ofkeyword{wedge} of the mesh. For the complete flow rate, these numbers have to 
be multiplied by the proportionality factor od total and wedge sections.}
\begin{eqnarray*}
	Q_s &=& \qty{6.35e-5}{m^3/s} 
	\\
	Q_m &=& \qty{5.58e-5}{m^3/s}
\end{eqnarray*}
According to that the dimensionless \textbf{entrainment ratio} is
\begin{equation*}
	M =  \frac{Q_s}{Q_m} = 1.14
\end{equation*} 

Pressure in the inlet patch can also be visualized (do it by yourself)

\subsection{Visualization in paraview}

Progress of water and air entrainment in the ejector can be seen in paraview, and even
an animation can be created with the \terminalcmd{foamCreateVideo} utility. Phase 
fraction and velocity distributions are shown in Figures \ref{fig:vofalphaplot}
and \ref{fig:vofuplot}.

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{Figures/VOFalphaPlot}
	\caption{Water-air fraction distribution for $t = \qty{0.2}{s}$}
	\label{fig:vofalphaplot}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{Figures/VOFUPlot}
	\caption{Velocity distribution for $t = \qty{0.2}{s}$.}
	\label{fig:vofuplot}
\end{figure}

\section{Part B: Euler-Euler Approach}

The case can be also downloaded from the GitHub repository.

\subsection{Running the simulation}
The step for mesh generation, setup and running of the solver are exactly the same. There are 
some key differences on the setup.

\begin{itemize}
	\item \textbf{Phase interaction description}. In VOF there is only a configuration file with surface
	tension. In Euler-Euler the interaction between forces has to be completely modeled 
	through the \filename{constant/phaseProperties} dictionary. For a detailed description of all the items, the great Master Thesis by Otromke\footnote{Otromke, Malte. 2013. “Implementation and Comparison of Correlations for Interfacial Forces in a Gas-Liquid System within an Euler-Euler Framework.”} can be consulted.
	\item \textbf{Fields files and initial condition}s. The main characteristic of Euler-Euler 
	approach is that fluid flow equations are \textbf{solved for both fluids}. That
	mains that two sets of field files are needed. For instance, there is a \filename{0/U.water} file but also a \filename{0/U.air} file.
	\item \textbf{Turbulence modeling}. Likewise, two turbulence modeling files are needed, one
	for each phase. They can be different, but compatible.
\end{itemize}

\subsection{Post-Processing in Terminal}

The command to see the residuals is the same as with the VOF solver


\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{Figures/EulerResiduals}
	\caption{Residuals for the Euler-Euler approach.}
	\label{fig:eulerresiduals}
\end{figure}

Follow the plots for flow rates in main and secondary patches.

\begin{figure}[H]
	\centering
	\includegraphics[width=1\linewidth]{Figures/EulerFlowRate}
	\caption{Flow rate in the secondary inlet.}
	\label{fig:eulerflowrate}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{Figures/EulerFlowRateMain}
	\caption{Flow rate in the main inlet}
	\label{fig:eulerflowratemain}
\end{figure}

Note that the entrainment ratio computation gives a very similar value to that of VOF
simulation

\subsection{Visualization with paraview}


Let's make a couple of filters to make better visualization. First, 
\terminalcmd{Reflect} filter is used to show the complete section of the ejector. 
Figure \ref{fig:euleralphaplot} show the fraction of water.

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{Figures/EulerAlphaPlot}
	\caption{Volume fraction of water for the Euler-Euler approach}
	\label{fig:euleralphaplot}
\end{figure}

Secondly, to see velocity we have to decide between water 
(Figure \ref{fig:euleruplot}.a) and air (Figure \ref{fig:euleruplot}.b).


\begin{figure}[H]
	\centering
	a)
	\includegraphics[width=0.45\linewidth]{Figures/EulerUwaterPlot}
	b)
	\includegraphics[width=0.45\linewidth]{Figures/EulerUairPlot}
	\caption{Velocity distribution for water and air phases.}
	\label{fig:euleruplot}
\end{figure}

But they can be combined by using the \terminalcmd{Calculator} filter and entering the
expression

\begin{equation}
	\mathbf{U} = \alpha_w\mathbf{U}_w + \alpha_a\mathbf{U}_a
\end{equation}

\begin{center}
	\includegraphics[width=0.3\linewidth]{Figures/EulerCalculator}
\end{center}


Velocity distribution shown in Figure \ref{fig:euleruplot2} is obtained, which is 
quite similar to the one in Figure \ref{fig:vofuplot}.

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{Figures/EulerUPlot}
	\caption{Combined velocity of both phases.}
	\label{fig:euleruplot2}
\end{figure}


% ========== SECTION 7: EXERCISES ==========
\section{Student Exercises and Extensions}

\subsection{Basic exercises for Part A: VOF}

\begin{enumerate}
	\item Effect of Inlet Velocity
	
	Modify the water inlet velocity from \qty{2}{m/s} to \qty{4}{m/s}. How does
	entrainment ratio change? Is the relationship linear?
	
	\item Effect of Surface Tension
	
	Set surface tension to zero. How does the interface shape change? How does $M$ 
	change? 
\end{enumerate}

\subsection{Basic exercises for Part B: Euler-Euler}

\begin{enumerate}
	\item Drag Model comparison
	
	Change drag model for \ofkeyword{(air in water)} from 
	\ofkeyword{SchillerNaumann} to \ofkeyword{TomiyamaCorrelated}. A 
	value of 16 will be need in the keyword \ofkeyword{A} (see Master Thesis
	by Otromke)
	
	\item Turbulent Dispersion
	
	In this tutorial, turbulent dispersion has not been considered. However, it is
	an important factor for high Reynolds number flows. Include the usual Burn's 
	model, with a Schmidt number $\sigma=1.0$.
	
\end{enumerate}


% ========== DISCLAIMER ============

\input{../disclaimer.tex}

% ========== END DOCUMENT ==========
\end{document}