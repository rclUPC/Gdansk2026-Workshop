# To create a public branch:

The key command are:
- create a new branch `git checkout -b public-release`
- create a new worktree `git worktree add ../Gdansk2006-Public public-release`. Hay que notar que el nuevo wrk tree esta **fuera del repositorio**



